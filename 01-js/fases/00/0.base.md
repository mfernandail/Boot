# Lenguaje interpretado

Un lenguaje interpretado es aquel cuyo código se ejecuta “sobre la marcha”,
sin generar primero un archivo ejecutable final como los lenguajes compilados clásicos.

## Lenguaje compilado (ej: C, C++)

1. Escribes el código
2. Un compilador lo traduce ANTES
3. Se genera un archivo ejecutable
4. El sistema ejecuta ese archivo

```js
Código → Compilador → Ejecutable → Ejecución
```

📌 Si hay un error grave, ni siquiera corre.

## Lenguaje interpretado (ej: JavaScript, Python)

1. Escribes el código
2. Un intérprete lo lee
3. Ejecuta línea por línea
4. No hay ejecutable final

📌 El código se analiza y ejecuta al mismo tiempo.

## JavaScript es interpretado en concepto

Pero compilado en la práctica (JIT).
Los motores modernos usan compilación Just-In-Time (JIT) para optimizar la ejecución.
Esto significa que aunque JavaScript es un lenguaje interpretado, los motores como V8 (Chrome, Node.js) y SpiderMonkey (Firefox) compilan partes del código en tiempo de ejecución para mejorar el rendimiento.

## Qué hace JavaScript realmente (sin tecnicismos)

Cuando tu código corre:

1. JS empieza simple
2. Ejecuta el código
3. Observa qué partes se repiten
4. Las vuelve más rápidas
5. Guarda esa versión optimizada
6. La usa las próximas veces

👉 Aprende mientras corre.

```js
function suma(a, b) {
  return a + b
}

for (let i = 0; i < 1000000; i++) {
  suma(1, 2)
}
```

Lo que hace JS:

- Primera vez: “ejecuto normal”
- Ve que suma se usa mucho
- Dice: “vale la pena optimizarla”
- La convierte a versión rápida
- La reutiliza

📌 Tú no ves nada.
📌 Pero el motor sí.

JavaScript empieza lento, aprende qué código usas más, y lo vuelve rápido mientras el programa ya está corriendo.

JIT es JavaScript aprendiendo y mejorando su propio rendimiento mientras ya está corriendo.

## Por qué seguimos diciendo “interpretado”

Porque:

- No hay un paso de compilación separado
- No generas un .exe
- El código se ejecuta dentro de un motor
- Puedes escribir JS y correrlo de inmediato

👉 Desde el punto de vista del desarrollador, se siente interpretado.

JavaScript es un lenguaje interpretado de alto nivel que utiliza compilación Just-In-Time para optimizar la ejecución, sin generar un binario final.

---

# ¿Qué es Just-In-Time (JIT) en palabras simples?

JIT significa que JavaScript traduce tu código a algo rápido justo en el momento en que lo necesita, no antes y no todo de una vez.

## El problema que JIT intenta resolver

Imagina dos extremos:

Opción A – Traducir TODO antes:

- Tarda mucho en empezar
- Pero luego corre rápido

Opción B – Traducir TODO sobre la marcha:

- Empieza rápido
- Pero corre más lento

👉 JIT mezcla lo mejor de ambos.

## Analogía MUY clara 🍳 (cocinar)

### 🧊 Lenguaje compilado (C)

- Cocinas todo
- Guardas
- Sirves después

### 🔥 Lenguaje interpretado clásico

- Cocinas cada plato cuando te lo piden
- Siempre igual de lento

### ⚡ JavaScript con JIT

- irves rápido
- Si un plato se pide muchas veces…
- 👉 aprendes a hacerlo más rápido
- 👉 lo optimizas

📌 Eso es JIT.

## Qué hace JavaScript realmente (sin tecnicismos)

Cuando tu código corre:

1. JS empieza simple
2. Ejecuta el código
3. Observa qué partes se repiten
4. Las vuelve más rápidas
5. Guarda esa versión optimizada
6. La usa las próximas veces

👉 Aprende mientras corre.

```js
function suma(a, b) {
  return a + b
}

for (let i = 0; i < 1000000; i++) {
  suma(1, 2)
}
```

Lo que hace JS:

- Primera vez: “ejecuto normal”
- Ve que suma se usa mucho
- Dice: “vale la pena optimizarla”
- La convierte a versión rápida
- La reutiliza
- 📌 Tú no ves nada
- 📌 Pero el motor sí.

## Por qué se llama Just-In-Time

Porque:

- ❌ No lo hace antes
- ❌ No lo hace todo
- ✅ Lo hace justo cuando hace falta

Traducción literal: “Justo a tiempo”

## Qué NO es JIT (importante)

❌ No crea un archivo ejecutable
❌ No es multihilo
❌ No hace magia
❌ No lo controlas tú

Es automático.
